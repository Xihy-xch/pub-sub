#include <bits/stdc++.h>
#include <pthread.h>
#include <unistd.h>

#include <map>
#define HOST_IP "127.0.0.1"
using namespace std;

int main() {

  string msg = "123456789";
  cout << msg.size() << endl;
  cout << sizeof(msg) << endl;

  return 0;
}